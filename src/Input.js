import React, { Component } from "react";

const Input = () => {
  return (
    <div>
      <input type="text" method="post"></input>
      <button>Add</button>
    </div>
  );
}

export default Input;